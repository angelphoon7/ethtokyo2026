# POST-GATE ONE-PROMPT BUILD PACKET AND MVP IMPLEMENTATION

**Run in Codex from the actual Git repository root only after the 07B HOLD has been explicitly resolved.** Supply these current files: `00_HACKATHON_CONTEXT.md`, `07_PROJECT_FREEZE.md`, `07B_POST_FREEZE_PROBLEM_VALIDATION.md`, `07C_HOLD_SPIKE_REPORT.md`, and the human's written go/no-go decision (`07D_GO_NO_GO_DECISION.md`, or an explicit decision in this message). If operator evidence exists, supply the dated notes as well. Do not substitute the older merchant-authority/ENS project files.

---

Act as the hands-on engineering lead. Build the **scoped agent x402 purchase guard with a live Intercepta pre-sign decision** specified by the attached freeze, subject to the subsequent validation and spike evidence. Work in the repository: create the necessary build documents, source code, configuration, tests, and demo. A completed Markdown packet is not a completed product. Preserve truthful scope and a meaningful commit history.

## A. Entry gate — do this before creating a build packet

1. Read every supplied file and inspect the existing repository and version-control history. Treat 07B's HOLD as controlling until the later evidence and human decision specifically resolve it. Note whether any earlier code or assets are eligible for the selected event track; do not rebrand pre-event project work as Start Fresh work.
2. If 07C is missing, BLOCKED/FAIL, or lacks a **real live risk decision, an actually settled allowed x402 payment, and evidence that held or bypassed cases create zero payer authorization signatures**, do not start the full build. State the smallest remaining spike with its pass/fail test. Do independent read-only or documentation work that helps resolve it.
3. If the technical spike passes but real operator need remains unvalidated, continue **only** if the human explicitly chooses a limited Intercepta partner-prize demonstration. Label the product-need and finalist claims unproven throughout the build. If the human selects STOP, stop. If both technical and operator gates are supported, proceed within the evidence-backed scope.
4. Verify the current official ETHGlobal Tokyo event rules, the selected Intercepta prize requirements, x402 buyer/seller SDK documentation, selected network/token and package APIs before locking interfaces. Cite dated source links in the project notes. Do not invent API response fields, signature formats, supported chains or prize eligibility. Keep a truthful record of meaningful human design, review, testing and demo contributions; AI output alone must not be presented as human work. If using this spec-driven workflow, include the actual planning artifacts and prompts in the submission repository as required by the event.

## B. Create a compact, complete build packet in the repository

Create or update the following files; keep a single authoritative copy of each, reconcile existing files instead of duplicating them, and avoid pages of filler:

- `docs/PRD.md`: exact owner and one paid task, target user, scope, positive and hold journeys, acceptance criteria, explicit non-goals, honest limitations, and frozen MVP. Distinguish sponsor-demo value from independently validated customer need.
- `docs/ARCHITECTURE.md`: components and trust boundaries; 402 requirement selection, endpoint/network/token/amount/recipient/task-budget policy, live address scan, decision mapping, signer isolation, authorization matching, settlement/reconciliation, error states and minimum data model. Include a small sequence diagram for approved and held paths. Record verified implementation choices and open dependencies.
- `docs/SPONSOR_AND_RULES.md`: exact prize checklist with links and where the actual code/demo proves each item; supported event track, reused-work inventory, and AI contribution disclosure. Never mark an unobserved requirement as satisfied.
- `docs/TEST_AND_DEMO.md`: executable acceptance cases, observed versus fixture-backed claims, 2–4 minute demo order, expected evidence for allowed/held/unknown/bypass/changed-quote outcomes, and transaction or API trace references once available.
- `docs/BUILD_PLAN.md`: smallest dependency-ordered tasks, owner/agent responsibilities, concrete done criteria, and time-critical path. Prioritize one end-to-end integration and a legible demo over broad features.
- `STATUS.md`: current phase, last verified command and result, open blockers, next action, and evidence references. Update after meaningful milestones.
- `AGENTS.md` **at repository root**: short project instructions for Codex: read the above docs and freeze, build the approved MVP end to end, enforce the sole protected signing path, never fabricate a live Intercepta call/payment, use the exact selected x402 SDK, never expose keys, run the repository's actual test/build commands, update STATUS, and report blockers honestly. Record the actual commands and file locations once established. This file must guide implementation rather than merely restate a product pitch.
- `CLAUDE.md` **uppercase at repository root**: start with the literal line `@AGENTS.md` so Claude Code loads the same shared instructions. Then give any brief Claude-specific direction: continue through the approved build plan, inspect code and docs before editing, verify acceptance criteria, and update `STATUS.md`. Do not duplicate or contradict `AGENTS.md`. The requested lowercase `claude.md` is not the conventional auto-loaded Claude Code file; use the uppercase filename for the functioning project instruction.
- `README.md`: setup, actual environment variables, how to run and test, live sponsor integration locations, allowed/held demonstration, API feedback, eligibility/AI disclosure and honest limitations. Create `.env.example` without secrets.

These documents are **scaffolding for execution**, not a reason to defer code. After writing them, perform a consistency check against the evidence and proceed directly to implementation in the same Codex task if the entry gate permits it.

## C. Build the approved smallest complete product

Implement only one owner-scoped task and one supported x402 payment scheme/network/token. Obtain a real 402 from a working paid endpoint. The agent may propose a purchase but never touch the payer key. The only signing route must independently check selected endpoint and full payment requirement, task and per-call budget, then call Intercepta on the **actual selected `payTo`** before signing; use the live documented response to allow or hold. Recheck the screened transfer fields at signing and invalidate a changed quote. Unknown/error must not authorize a payment. Reconcile uncertain settlement instead of blindly retrying.

Produce an allowed branch with a real paid response and inspectable settlement evidence; produce a locally permitted but **live-risk-held** controlled branch with a visible reason and zero authorization-signing calls. Label sponsor fixture addresses as controlled examples, never as proof of a common real-world attack. Provide a small owner-facing page or clear demo trace showing requested resource, quote, decision, cost/status and usable or held task outcome. Do not add ENS, fallback provider shopping, new chains, or extra sponsor dependencies without an explicit scope decision.

Test the meaningful invariants: protected signer cannot be bypassed through the automatic buyer wrapper or another exposed wallet tool; `payTo`, amount, asset, network or validity changes invalidate approval; held/unknown/error creates zero signatures; budget cannot be exceeded by concurrent requests; and the allowed payment genuinely settles. Keep secrets out of commits, logs, and documentation. Mock only in isolated unit tests, label those tests, and preserve separate evidence for live integration. Keep reviewable commits and record AI-assisted work as the actual event rules require.

## D. Final output and blocking behavior

Run real build/tests and one end-to-end live demonstration if credentials, wallet, test funds, seller and network are available. Update the packet with observed results. Finish with: repository paths, changed files, commands and pass/fail outputs, live transaction or settlement and sanitized risk-evidence references, unmet sponsor requirements, and the precise demo/submission readiness status. If a required key, funding, test seller, chain access or gate decision is missing, complete all non-dependent implementation, identify exactly what the human must provide, and mark that portion **BLOCKED**. Never claim completion based on generated docs, screenshots, simulated risk labels, or unit tests alone.
